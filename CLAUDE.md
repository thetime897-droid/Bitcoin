# Marktupdate by Panda_investiert (Remotion)

Tägliches Börsen-Marktupdate als Video (Short 9:16 + Long 16:9).

- Tägliche Folge erstellen: exakt `ROUTINE.md` befolgen.
- Nur `src/data/episode.ts` ändert sich täglich; Design/Code nur bei Fehlern anfassen.
- Rendern in der Cloud: `bash scripts/cloud-render.sh` (setzt Browser/ffmpeg selbst auf).
- Kommunikation mit dem Nutzer auf Deutsch.
